<?xml version="1.0" encoding="utf-8"?>
<!-- Screen B of the cross-screen preload demo. The ad is warmed on MainActivity (screen A) via
     NativeAdPreloader.preload(); this screen binds it instantly with NativeAdPreloader.showInto()
     — no second request, no shimmer wait, when the preload has landed. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/preloadedNativeRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ng_ink"
    android:orientation="vertical"
    tools:context=".PreloadedNativeActivity">

    <!-- ===================== Header ===================== -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="20dp"
        android:paddingTop="20dp"
        android:paddingEnd="20dp"
        android:paddingBottom="16dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:text="Preloaded native (cross-screen)"
            android:textColor="@color/ng_text"
            android:textSize="22sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvPreloadedStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Warmed on the previous screen — binding instantly here."
            android:textColor="@color/ng_muted"
            android:textSize="13sp" />
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/ng_line" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:fillViewport="true"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                style="@style/Text.NextGen.SectionTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:text="NativeAdPreloader.showInto(this unit)" />

            <!-- The template is pre-configured so showInto()'s shimmer matches the final ad. -->
            <com.alihassan.nextgenads.nativead.NativeTemplateView
                android:id="@+id/preloadedNative"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                app:ngad_template="medium" />

            <Button
                android:id="@+id/btnReloadPreloaded"
                style="@style/Widget.NextGen.Button.Primary"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="28dp"
                android:text="Show again" />
        </LinearLayout>
    </ScrollView>
</LinearLayout>
