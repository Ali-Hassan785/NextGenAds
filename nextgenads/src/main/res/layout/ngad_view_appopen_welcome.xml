<?xml version="1.0" encoding="utf-8"?>
<!-- Full-screen "Welcome back" cover shown during the app-open flow. Attached to the host activity's
     decor view (not a Dialog) so it fills the whole screen and fades smoothly into the ad. The app
     icon + label are populated at runtime by AppOpenAdHelper. Theme-aware via the ngad_* palette. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface"
    android:clickable="true"
    android:focusable="true">

    <!-- Centered brand block: icon, welcome title, state subtitle. -->
    <LinearLayout
        android:id="@+id/ngad_appopen_content"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingStart="40dp"
        android:paddingEnd="40dp">

        <FrameLayout
            android:layout_width="96dp"
            android:layout_marginTop="4dp"
            android:layout_height="96dp"
            android:background="@drawable/ngad_bg_welcome_icon"
            android:elevation="2dp">

            <ImageView
                android:id="@+id/ngad_appopen_icon"
                android:layout_width="60dp"
                android:layout_height="60dp"
                android:layout_gravity="center"
                android:contentDescription="@string/ngad_welcome_icon_desc"
                android:importantForAccessibility="no" />
        </FrameLayout>

        <TextView
            android:id="@+id/ngad_appopen_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            android:includeFontPadding="false"
            android:letterSpacing="-0.01"
            android:text="@string/ngad_welcome_title"
            android:textColor="?attr/colorOnSurface"
            android:textSize="26sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/ngad_appopen_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:includeFontPadding="false"
            android:layout_marginTop="8dp"
            android:text="@string/ngad_welcome_loading"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:textSize="16sp" />
    </LinearLayout>

    <!-- Slim progress pinned near the bottom so it reads as "app is preparing", not "ad is loading". -->
    <ProgressBar
        android:id="@+id/ngad_appopen_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal|bottom"
        android:layout_marginBottom="56dp"
        android:indeterminate="true"
        android:indeterminateTint="@color/ngad_cta" />
</FrameLayout>
