<?xml version="1.0" encoding="utf-8"?>
<!-- Media/icon surface (square corners). Used as the MediaView and icon background + outline; with
     the code-set clipToOutline the media is clipped to these square corners, so neither the media
     nor the icon is rounded in any native template. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="?attr/colorSurfaceVariant" />
</shape>
